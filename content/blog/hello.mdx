---
title: "Hello, world"
description: "A placeholder post to verify the MDX pipeline works."
date: "2026-05-23"
tags: ["meta"]
---

This is a placeholder post. Replace the file in `content/blog/hello.mdx` with
real content — or delete it — when you're ready.

## Code blocks render

```ts
function greet(name: string) {
  return `hello, ${name}`;
}
```

## Lists work

- One
- Two
- Three

[External links open in a new tab](https://nextjs.org).
